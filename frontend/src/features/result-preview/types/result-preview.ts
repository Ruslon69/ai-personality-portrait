export type PreviewObservation = {
  description: string;
  id: string;
  source: string;
  title: string;
};

export type PreviewRecommendation = {
  description: string;
  id: string;
  title: string;
};

export type FullPreviewSection = {
  description: string;
  id: string;
  title: string;
};
